type featuredTasks = {
    id: string,
    categoryId: string,
    name: string,
    description: string,
    userImageURL: string
}

type taskCategory = {
    id: string,
    name: string,
    description: string,
    defaultURL: string,
    themeColor: string
}